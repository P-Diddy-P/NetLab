from sys import argv

"""
    First argv is the polyploid file, with all subsequent argvs are
    network directories (first one is IWDB requiring conversion).
"""

if __name__ == "__main__":
    print(argv)
